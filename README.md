# BookClub Microservices

Учебный проект для лабораторных работ по микросервисной архитектуре.  
Приложение разделено на независимые сервисы, оркестрируется через Docker Compose, покрыто юнит‑тестами и собирается/деплоится через GitHub Actions и Docker Hub.

---

## 📦 Архитектура

Проект состоит из 5 микросервисов:

- **auth-service** — регистрация, логин, JWT‑токены, роли, бан пользователей
- **users-service** — «полки» пользователей: «Хочу прочитать», «Прочитано»
- **books-service** — каталог книг: добавление, редактирование, жанры, описание, ТОП
- **reviews-service** — комментарии и оценки к книгам
- **api-gateway** — веб‑интерфейс, маршрутизация, рендер страниц (EJS), общение с сервисами

Каждый сервис независимый, имеет свой `Dockerfile` и тесты.

---

## ⚙️ Оркестрация

Все сервисы описаны в `docker-compose.yml`.  
Compose поднимает их вместе и связывает в одной сети:

```bash
docker-compose up --build
